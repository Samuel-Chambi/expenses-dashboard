# Budget Tracking — Applicable Standards

## global/project-structure
Feature-based layout. Budgets feature in src/features/budgets/.

## api/server-actions
Server Actions for mutations. Zod validation. getCurrentUserId(). ActionResult type.

## database/prisma
Singleton client. Per-user data (userId). Hard delete for budgets (no history needed).

## frontend/ui-components
shadcn/ui, Tailwind, react-hook-form + Zod, Lucide icons, Sonner toasts.
