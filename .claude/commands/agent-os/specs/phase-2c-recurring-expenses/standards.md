# Recurring Expenses — Applicable Standards

## global/project-structure
Feature-based layout. Recurring feature in src/features/recurring/.

## api/server-actions
Server Actions for mutations. Zod validation. getCurrentUserId(). ActionResult type.

## database/prisma
Singleton client. Soft deletes. Per-user data (userId). updateMany for ownership.

## frontend/ui-components
shadcn/ui, Tailwind, react-hook-form + Zod, Lucide icons, Sonner toasts.
